# Configurações do EmailJS
# Copie este arquivo para .env e preencha com seus valores reais

# Como obter essas credenciais:
# 1. Acesse https://www.emailjs.com/
# 2. Crie uma conta gratuita
# 3. Configure um serviço de email (Gmail, Outlook, etc.)
# 4. Crie um template de email
# 5. Obtenha as credenciais abaixo

# Service ID do EmailJS (ex: service_abc123)
EMAILJS_SERVICE_ID=your_service_id_here

# Template ID do EmailJS (ex: template_xyz789)
EMAILJS_TEMPLATE_ID=your_template_id_here

# User ID (Public Key) do EmailJS (ex: user_def456)
EMAILJS_USER_ID=your_user_id_here

# Email de destino para receber os registros
DESTINATION_EMAIL=coordenacao@escola.edu.br

# Configurações de Upload de Arquivos
# Tamanho máximo do arquivo em MB (padrão: 5)
MAX_FILE_SIZE_MB=5

# Tipos de arquivo permitidos (separados por vírgula)
# Formatos suportados: PDF, imagens (JPG, PNG), documentos Word
ALLOWED_FILE_TYPES=.pdf,.jpg,.jpeg,.png,.doc,.docx